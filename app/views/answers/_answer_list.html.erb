<thead>
  <tr>
    <th>title</th>
    <th>answer</th>
    <th>time</th>
    <th></th>
    <th></th>
  </tr>
</thead>
<tbody>
  <% question.answers.each do |answer| %>
    <tr id="answer-<%= answer.id %>">
      <td>
        <%= answer.title %>
      </td>
      <td>
        <%= answer.content %>
      </td>
      <td>
        <%= answer.created_at.strftime("%Y/%m/%d") %>
      </td>
      </td>
      <td id="like-<%= answer.id %>">
        <%= render 'likes/like', likeable: answer %>
      </td>
      <td>
        <% if answer.user == current_user %>
          <%= link_to "Destroy", answer_path(answer), method: :delete, remote: true, class:"btn  btn-success btn-sm", "data-confirm" => "本当に削除してもよろしいですか？" %>
        <% end %>
      </td>
    </tr>
  <% end %>
</tbody>
